export default function(){
  // TODO: replace the mock
  return window.location.origin.indexOf('http://localhost') > -1 // KARD: mock the detection
  return window.location.origin.indexOf('ptscrm.com') > -1
}